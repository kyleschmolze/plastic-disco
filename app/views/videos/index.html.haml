.page-header.text-center
  %h1 Videos
  %p This archive is mostly used for re-aligning timestamps.

= will_paginate @videos, renderer: BootstrapPagination::Rails

%table.table.table-middle
  - for video in @videos
    %tr
      %td
        = link_to video_path(video) do
          = image_tag video.thumbnail, style: 'height: 50px;'
      %td
        = link_to video_path(video) do
          #{video.starts_at.strftime('%b %e, %l:%M:%S %p')} -
          #{video.ends_at.strftime('%l:%M:%S %p')}

      %td
        %small.text-muted
          #{video.duration/60}m#{video.duration%60}s

      %td
        %small.text-muted
          #{video.events.length} events

      %td
        %small.text-muted= video.youtube_id ? 'youtube' : 'drive'

      %td
        - if video.hidden?
          %small.text-danger
            %i.fa.fa-remove
            Hidden
        - elsif video.aligned?
          %small.text-success
            %i.fa.fa-check
            Aligned
        - else
          %small.text-warning
            %i.fa.fa-warning
            Not aligned

      %td.text-right
        %small.text-muted
          = video.title


= will_paginate @videos, renderer: BootstrapPagination::Rails
